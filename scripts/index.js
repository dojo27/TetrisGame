let girls = ['Geri Halliwell', 'Emma Bunton', 'Melanie B', 'Melanie C', 'Victoria Beckham'];
for(let i = 0; i < girls.length; i++)
    console.log((i + 1) + ' ' + girls[i]);

    var a = 5;
var b = 10;
console.log(`Fifteen is ${a + b} and not ${2 * a + b}.`);

const fs = require('fs');
fs.open('text.txt');